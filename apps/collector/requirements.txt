# Aggregate for the dev venv: COLLECTOR=all runs all three collectors in one
# process (npm run collector / dev:all), so the venv needs every dependency.
# The prod images each install only their own requirements-<collector>.txt.
-r requirements-meter.txt
-r requirements-sma.txt
-r requirements-wallbox.txt
